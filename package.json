{
  "name": "skymap",
  "version": "0.1.0",
  "description": "Skymap — navigate the Minneapolis Skyway, the largest contiguous skyway network in the world.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/build-sw.mjs",
    "preview": "vite preview",
    "data:seed": "node scripts/generate-seed-data.mjs",
    "data:osm": "node scripts/fetch-osm.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "maplibre-gl": "^5.24.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "playwright-core": "^1.61.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}
